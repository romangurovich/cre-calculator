{
  "name": "cre-calc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
